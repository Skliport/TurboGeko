CREATE TABLE IF NOT EXISTS payment_employee(
	id_payment_employee SERIAL PRIMARY KEY,
	id_employee int not null,
	payment_date date not null,
	salary real not null,
	isss real not null,
	afp real not null,
	notes varchar(60)
);


CREATE TABLE IF NOT EXISTS fixed_costs(
	id_fixed_costs serial primary key,
	cost_name varchar(30),
	tax real,
	subtotal real,
	total real
) 

/*Consultas*/

SELECT * FROM finished_product;

SELECT * FROM material;

SELECT pe.id_payment_employee, emp.first_name,pe.payment_date, 
((pe.afp *pe.salary) /100)+((pe.isss*pe.salary)/100) AS descuento,pe.salary,
pe.salary-(((pe.afp *pe.salary) /100)+((pe.isss*pe.salary)/100)) AS total
FROM employee emp
INNER JOIN  payment_employee pe ON emp.id_employee = pe.id_employee
ORDER BY pe.id_payment_employee ASC;




--Más consultas
--tablas de los demas modulos





 

